# Workspace Guidelines & Rules

## Commit and Push Policy
- Always stage (`git add .`), commit (`git commit -m "..."`), and push (`git push origin main`) to remote after completing any coding task, fix, or feature request.
